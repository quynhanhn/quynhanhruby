<%= form_with(model: subject) do |form| %>
<% if subject.errors.any? %>
<div id="error_explanation" class="bg-red-50 p-6 rounded text-red-800">
  <h2 class="text-lg font-semibold mb-3"><%= pluralize(subject.errors.count, "error") %> prohibited this subject from being saved:</h2>

  <ul class="list-disc leading-relaxed">
    <% subject.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

  <div class="mb-6">
        <%= form.label :bear, class: label_class %>
          <%= form.text_field :bear, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :white, class: label_class %>
          <%= form.number_field :white, class: input_class %>
      </div>

<%= form.submit class: button_class(theme: 'primary') %>

<% if subject.persisted? %>
<%= link_to "Cancel", subject, class: "text-neutral-700 underline inline-block ml-3" %>
<% else %>
<%= link_to "Cancel", subjects_path, class:  "text-neutral-700 underline inline-block ml-3" %>
<% end %>
<% end %>
